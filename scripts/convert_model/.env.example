# Copy this file to .env and fill in your values.
# .env is gitignored — never commit your token.

# Write-access token from https://huggingface.co/settings/tokens
HF_TOKEN=hf_...

# Target HuggingFace repo (will be created if it doesn't exist)
HF_REPO_ID=your-username/paddleocr-vl-manga-mlx
